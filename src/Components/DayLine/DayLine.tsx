import { FC } from "react";
// Components
// Context
// Hooks
// Pages
// Resources

export interface Props {
};

const DayLine: FC<Props> = ({}) => {

    return (
        <div>
            DayLine Component
        </div>
    );
};

export default DayLine;